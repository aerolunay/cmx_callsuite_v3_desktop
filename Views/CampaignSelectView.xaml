<UserControl x:Class="CmxDialer.Views.CampaignSelectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20,24,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Select campaign" FontSize="22" FontWeight="Bold"/>
            <TextBlock Text="{Binding Instruction}" Style="{StaticResource Muted}" FontSize="13" Margin="0,4,0,16"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Choices}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="0,0,0,8" Cursor="Hand"
                                      FocusVisualStyle="{x:Null}">
                                <CheckBox.Template>
                                    <ControlTemplate TargetType="CheckBox">
                                        <Border x:Name="Bd" Background="White" BorderBrush="{StaticResource LineBrush}"
                                                BorderThickness="1" CornerRadius="10" Padding="14,12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="SemiBold"
                                                               TextTrimming="CharacterEllipsis"/>
                                                    <TextBlock Text="{Binding TypeLabel}" Style="{StaticResource Muted}" Margin="0,2,0,0"/>
                                                </StackPanel>
                                                <Border x:Name="Tick" Grid.Column="1" Width="22" Height="22" CornerRadius="11"
                                                        BorderBrush="{StaticResource LineBrush}" BorderThickness="2" VerticalAlignment="Center">
                                                    <Path x:Name="TickMark" Data="{StaticResource IconCheck}" Width="11" Height="11"
                                                          Stretch="Uniform" Stroke="White" StrokeThickness="3"
                                                          StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                                          Visibility="Collapsed"/>
                                                </Border>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource CyanBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource CyanSoftBrush}"/>
                                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource CyanBrush}"/>
                                                <Setter TargetName="Bd" Property="BorderThickness" Value="2"/>
                                                <Setter TargetName="Tick" Property="Background" Value="{StaticResource CyanBrush}"/>
                                                <Setter TargetName="Tick" Property="BorderBrush" Value="{StaticResource CyanBrush}"/>
                                                <Setter TargetName="TickMark" Property="Visibility" Value="Visible"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </CheckBox.Template>
                            </CheckBox>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <TextBlock Text="Loading your campaigns…" Style="{StaticResource Muted}" FontSize="13"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="{Binding IsLoading, Converter={StaticResource BoolVis}}"/>
            <TextBlock Text="You're not assigned to any campaigns yet. Ask a supervisor to add you." Style="{StaticResource Muted}"
                       FontSize="13" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20,0"
                       Visibility="{Binding IsEmpty, Converter={StaticResource BoolVis}}"/>
        </Grid>

        <StackPanel Grid.Row="2" Margin="0,12,0,0">
            <Border Background="{StaticResource DangerSoftBrush}" CornerRadius="8" Padding="10,8" Margin="0,0,0,10"
                    Visibility="{Binding Error, Converter={StaticResource StringVis}}">
                <StackPanel>
                    <TextBlock Text="{Binding Error}" Foreground="{StaticResource DangerTextBrush}" FontSize="12" TextWrapping="Wrap"/>
                    <Button Content="Try again" Style="{StaticResource LinkButton}" Margin="0,6,0,0" HorizontalAlignment="Left"
                            Command="{Binding RetryCommand}"/>
                </StackPanel>
            </Border>
            <Button Content="Continue" Style="{StaticResource PrimaryButton}" Height="48" Command="{Binding ContinueCommand}" IsDefault="True"/>
            <Button Content="Cancel" Style="{StaticResource OutlineButton}" Margin="0,8,0,0" Command="{Binding CancelCommand}"
                    Visibility="{Binding IsChange, Converter={StaticResource BoolVis}}"/>
        </StackPanel>
    </Grid>
</UserControl>
